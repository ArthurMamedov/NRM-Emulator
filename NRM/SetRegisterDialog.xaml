<Window x:Class="NRM.SetRegisterDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NRM"
        mc:Ignorable="d"
        Title="SetRegisterDialog" Height="200" Width="450">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="Enter the values of registers through space" Margin="0 25 0 0" HorizontalAlignment="Center" FontWeight="Black"/>
            <TextBox Margin="20 10 20 0" TextAlignment="Center" x:Name="StringRegister" Height="25"/>
        </StackPanel>
        <Button Grid.Row="1" Content="Set registers" Margin="20 20 20 20" Click="SetRegister"/>
    </Grid>
</Window>
